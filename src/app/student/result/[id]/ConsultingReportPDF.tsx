import { Document, Page, Text, View, StyleSheet, Font } from '@react-pdf/renderer'

// Register Korean Font to avoid character breaking
Font.register({
    family: 'Pretendard',
    src: 'https://cdn.jsdelivr.net/gh/orioncactus/pretendard/packages/pretendard/dist/public/static/Pretendard-Medium.ttf',
})

// Define styling to look clean and professional
const styles = StyleSheet.create({
    page: {
        padding: 60,
        fontFamily: 'Pretendard',
        backgroundColor: '#ffffff',
    },
    headerBox: {
        borderBottom: '2pt solid #2563eb', // Clean blue accent
        paddingBottom: 20,
        marginBottom: 30,
    },
    mainTitle: {
        fontSize: 24,
        color: '#1e3a8a', // Dark blue
        fontWeight: 'bold',
        marginBottom: 8,
    },
    subTitle: {
        fontSize: 12,
        color: '#64748b',
        textTransform: 'uppercase',
        letterSpacing: 1,
    },
    metaSection: {
        marginBottom: 30,
        padding: 15,
        backgroundColor: '#f8fafc',
        borderRadius: 4,
    },
    metaRow: {
        flexDirection: 'row',
        marginBottom: 6,
    },
    metaLabel: {
        fontSize: 10,
        width: 100,
        color: '#64748b',
        fontWeight: 'bold',
    },
    metaValue: {
        fontSize: 11,
        color: '#0f172a',
        flex: 1,
    },
    contentTitle: {
        fontSize: 16,
        color: '#0f172a',
        fontWeight: 'bold',
        marginBottom: 15,
        marginTop: 10,
    },
    contentBody: {
        fontSize: 11,
        lineHeight: 1.6,
        color: '#334155',
    },
    footer: {
        position: 'absolute',
        bottom: 30,
        left: 60,
        right: 60,
        textAlign: 'center',
        fontSize: 9,
        color: '#94a3b8',
        borderTop: '1pt solid #e2e8f0',
        paddingTop: 10,
    }
})

type PDFProps = {
    studentName: string
    targetCompany: string
    finalContent: string
    date: string
}

export function ConsultingReportPDF({ studentName, targetCompany, finalContent, date }: PDFProps) {
    return (
        <Document>
            <Page size="A4" style={styles.page}>
                {/* HEADER */}
                <View style={styles.headerBox}>
                    <Text style={styles.mainTitle}>AI Job Navigator</Text>
                    <Text style={styles.subTitle}>Customized Consulting Report</Text>
                </View>

                {/* META INFORMATION */}
                <View style={styles.metaSection}>
                    <View style={styles.metaRow}>
                        <Text style={styles.metaLabel}>PREPARED FOR:</Text>
                        <Text style={styles.metaValue}>{studentName}</Text>
                    </View>
                    <View style={styles.metaRow}>
                        <Text style={styles.metaLabel}>TARGET ROLE:</Text>
                        <Text style={styles.metaValue}>{targetCompany}</Text>
                    </View>
                    <View style={styles.metaRow}>
                        <Text style={styles.metaLabel}>DATE:</Text>
                        <Text style={styles.metaValue}>{date}</Text>
                    </View>
                </View>

                {/* MAIN CONTENT */}
                <View>
                    <Text style={styles.contentTitle}>Consultant Feedback & AI Analysis</Text>
                    <Text style={styles.contentBody}>
                        {finalContent || 'No feedback was generated.'}
                    </Text>
                </View>

                {/* FOOTER */}
                <Text style={styles.footer} render={({ pageNumber, totalPages }) => (
                    `Generated by AI Job Navigator • Page ${pageNumber} of ${totalPages}`
                )} fixed />

            </Page>
        </Document>
    )
}
